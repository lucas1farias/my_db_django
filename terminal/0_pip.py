

"""
    pip install --upgrade pip
    pip install -r nome_arquivo.txt (instalar dependências de um arq. de dependências via terminal)
    pip uninstall -r nome_arquivo.txt (aula 107) (51:00) (desinstalar dependências de um arq. de dependências pelo terminal)
    pip install channels-redis (aula 87)
    pip install coverage
    pip install django
    pip install django-bootstrap4
    pip install django-channels (aula 87)
    pip install django-chartjs (aula 101)
    pip install django-stdimage
    pip install dj-static
    pip install geoip2 (aula 96)
    pip install gunicorn
    pip install model_mommy
    pip install mysqlclient
    pip install psycopg2-binary
    pip install PyMySQL
    pip install qrcode (/home/lucas/PycharmProjects/neural_nine/_qrcode.py)
    pip install reportlab (aula 105)
    pip install requests (aula 92)
    pip install textblob
    pip install virtualenv
    pip install virtualenvwrapper-win
    pip install WeasyPrint
    pip install whitenoise

    from textblob import TextBlob
    var = TextBlob('Uma frase em língua portuguesa')
    var.translate(to='ar')
    var.translate(to='en')
    var.translate(to='es')
    var.translate(to='fr')
    var.translate(to='ru')
    var.translate(to='zh-CN')
"""